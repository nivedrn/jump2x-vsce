{
  "name": "jump2x",
  "displayName": "Jump2x",
  "description": "Quickly open and manage favorite and discovered workspaces.",
  "version": "1.0.0",
  "publisher": "devn",
  "engines": {
    "vscode": "^1.90.0"
  },
  "icon": "images/icon.png",
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onView:jump2x.workspaceExplorer",
    "onCommand:jump2x.quickOpenWorkspace",
    "onCommand:jump2x.addCurrentWorkspace",
    "onCommand:jump2x.refreshWorkspaces",
    "onCommand:jump2x.openWorkspace",
    "onCommand:jump2x.removeFavorite",
    "onCommand:jump2x.hideWorkspace",
    "onCommand:jump2x.restoreWorkspace",
    "onCommand:jump2x.openSettings"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "views": {
      "explorer": [
        {
          "id": "jump2x.workspaceExplorer",
          "name": "Jump2x"
        }
      ]
    },
    "commands": [
      {
        "command": "jump2x.addCurrentWorkspace",
        "title": "Jump2x: Add Current Workspace to Favorites",
        "category": "Jump2x",
        "icon": "$(star-add)"
      },
      {
        "command": "jump2x.refreshWorkspaces",
        "title": "Jump2x: Refresh Workspaces",
        "category": "Jump2x",
        "icon": "$(refresh)"
      },
      {
        "command": "jump2x.openWorkspace",
        "title": "Jump2x: Open Workspace",
        "category": "Jump2x"
      },
      {
        "command": "jump2x.removeFavorite",
        "title": "Jump2x: Remove Favorite",
        "category": "Jump2x",
        "icon": "$(trash)"
      },
      {
        "command": "jump2x.hideWorkspace",
        "title": "Jump2x: Hide Workspace",
        "category": "Jump2x",
        "icon": "$(eye-closed)"
      },
      {
        "command": "jump2x.restoreWorkspace",
        "title": "Jump2x: Restore Workspace",
        "category": "Jump2x",
        "icon": "$(eye)"
      },
      {
        "command": "jump2x.openSettings",
        "title": "Jump2x: Open Settings",
        "category": "Jump2x",
        "icon": "$(gear)"
      },
      {
        "command": "jump2x.quickOpenWorkspace",
        "title": "Jump2x: Quick Open Workspace",
        "category": "Jump2x"
      }
    ],
    "keybindings": [
      {
        "command": "jump2x.quickOpenWorkspace",
        "key": "ctrl+shift+k",
        "mac": "cmd+shift+k"
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "jump2x.addCurrentWorkspace",
          "when": "view == jump2x.workspaceExplorer",
          "group": "navigation@1"
        },
        {
          "command": "jump2x.refreshWorkspaces",
          "when": "view == jump2x.workspaceExplorer",
          "group": "navigation@2"
        },
        {
          "command": "jump2x.openSettings",
          "when": "view == jump2x.workspaceExplorer",
          "group": "navigation@3"
        }
      ],
      "view/item/context": [
        {
          "command": "jump2x.removeFavorite",
          "when": "view == jump2x.workspaceExplorer && viewItem == favoriteWorkspace",
          "group": "inline"
        },
        {
          "command": "jump2x.addCurrentWorkspace",
          "when": "view == jump2x.workspaceExplorer && viewItem == discoveredWorkspace",
          "group": "inline"
        },
        {
          "command": "jump2x.hideWorkspace",
          "when": "view == jump2x.workspaceExplorer && viewItem == discoveredWorkspace",
          "group": "inline"
        },
        {
          "command": "jump2x.restoreWorkspace",
          "when": "view == jump2x.workspaceExplorer && viewItem == hiddenWorkspace",
          "group": "inline"
        }
      ],
      "commandPalette": [
        {
          "command": "jump2x.openWorkspace",
          "when": "false"
        },
        {
          "command": "jump2x.removeFavorite",
          "when": "false"
        },
        {
          "command": "jump2x.hideWorkspace",
          "when": "false"
        },
        {
          "command": "jump2x.restoreWorkspace",
          "when": "false"
        }
      ]
    },
    "configuration": {
      "title": "Jump2x",
      "properties": {
        "jump2x.workspacesDirectories": {
          "type": "array",
          "items": { "type": "string" },
          "default": [],
          "description": "Directories to scan for git repositories and .code-workspace files. Supports multiple paths."
        },
        "jump2x.excludedDirectories": {
          "type": "array",
          "items": { "type": "string" },
          "default": [],
          "description": "Directories to skip during scanning. Full paths that the scanner should not traverse."
        },
        "jump2x.recursiveScan": {
          "type": "boolean",
          "default": true,
          "description": "Recursively scan nested folders in workspacesDirectories."
        },
        "jump2x.maxScanDepth": {
          "type": "number",
          "default": -1,
          "description": "Maximum folder depth to scan from each root directory. Depth is measured in folder levels from the configured root (0 = root only, 1 = include direct children). Use -1 for unlimited depth."
        },
        "jump2x.includeCodeWorkspaceFiles": {
          "type": "boolean",
          "default": true,
          "description": "Include discovered .code-workspace files."
        }
        ,
        "jump2x.quickPickItemLimit": {
          "type": "number",
          "default": 20,
          "description": "Maximum number of items to show in the quick open dropdown (favorites + discovered). Set to -1 for unlimited."
        }
      }
    }
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "test": "npm run compile && node --test test/workspace-utils.test.js",
    "vscode:prepublish": "npm run compile",
    "release:patch": "npm version patch -m 'chore: release v%s' && git push && git push --tags",
    "release:minor": "npm version minor -m 'chore: release v%s' && git push && git push --tags",
    "release:major": "npm version major -m 'chore: release v%s' && git push && git push --tags",
    "package:vsix": "vsce package --allow-missing-repository --skip-license"
  },
  "devDependencies": {
    "@vscode/vsce": "^3.6.2",
    "@types/node": "^20.14.2",
    "@types/vscode": "^1.90.0",
    "typescript": "^5.5.4"
  }
}
